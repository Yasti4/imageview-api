'use strict';

module.exports = `
    ${require('./userInsert.input')}
    ${require('./userUpdate.input')}
    ${require('./UserFollow.input')}
    ${require('./privacityInsert.input')}
    ${require('./privacityUpdate.input')}
`